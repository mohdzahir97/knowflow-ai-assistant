# Base URL of the FastAPI backend. Must be reachable from the browser, so
# inside Docker this is the published host address, not a Compose service name
# (unlike the Streamlit client, which calls the API from the server side).
VITE_API_BASE_URL=http://localhost:8001
